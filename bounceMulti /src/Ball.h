#pragma once

#include "ofMain.h"
#include "Base.h"

class Ball : public Base {     // set the class to inherit from Base
    
public:
    
    virtual void draw();            // method that we want to be different from the 'mother class'
    
private:
    
};
